./install.sh

# Repo-only notes, not dotfiles: these must not render into $HOME.
./TODO.md

# Local-only secret: OpenRouter API key referenced by dot_dsh/settings.yaml.
# Never add this to the repo; recreate per machine (format documented in
# dot_dsh/settings.yaml).
.dsh/.credentials.yaml
