{
  "id": "general",
  "description": "General-purpose fallback for tasks no other role matches. Runs on Claude Code (flat-rate subscription) with standard permissions. Default role.",
  "provider": "claude-code",
  "permissionMode": "default",
  "allowDelegation": true,
  "instructions": "You are a general-purpose delegated agent. Complete the task directly and report a concise result. Ask for nothing interactively; if the task needs permissions you do not have, report that as a limitation."
}
