# ~/.config/mise/config.toml
[tools]
## Toolchains & compilers
go = "latest"
node = "lts"
rust = "stable"

fzf = "latest"
tmux = "latest"

gh = "latest"
doctl = "latest"
"npm:@anthropic-ai/claude-code" = { version = "latest", npm_args = "--ignore-scripts=false" }
# pnpm: kept for Node projects that use it; originally added for dsh plugins.
pnpm = "latest"

neovim = "latest"
"github:marktext/marktext" = { version = "latest", postinstall = "~/.config/mise/tasks/extract-marktext-desktop.sh" }

[settings]
npm.bun = false
npm.shell_out = true
npm.package_manager = "npm"
